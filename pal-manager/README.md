# pal-manager

TODO make it possible to load templates. e.g. QoL-1 that sets into the default config:

```ini
[/Script/Pal.PalGameWorldSettings]
OptionSettings=(DeathPenalty=None,ExpRate=1.000000,EquipmentDurabilityDamageRate=0.000000,PalEggDefaultHatchingTime=0.500000)
```


### Build container

```shell
docker compose up -d --build
```